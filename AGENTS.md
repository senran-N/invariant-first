# 维护 invariant-first

此文件约束对本 Skill 仓库的修改，不给使用本 Skill 的其他项目增加开发流程。

## 初衷

让 AI 像对结果负责的开发者一样完成工作：果断实现、架构清楚、文档可用、跨会话可接手。减少过度防御、假想兼容、反复计划与无效补丁，不以更多提示词、工具调用、测试或报告证明进步。具体工程原则只在 [RULES.md](RULES.md) 维护。

## 修改落点

- 路由条件、路径与加载优先级：只改 `config/routing.json`，用 `scripts/catalog.py` 生成两份视图。
- 行动方法：改对应 `skills/if-*/SKILL.md`。独立触发和独立行动都成立时才新增技能；不要为一个补丁再造框架。
- 普通任务的默认上下文保持小；复杂度放到真正需要时才读的模块。保留直接读取方法的路径，不把脚本、Python 或子代理变成必需依赖。
- 修改文档与代码的真实行为一起收尾。单入口版本由 `scripts/export_single.py` 导出，不手改第二份规则；导出不带仓库历史或缓存。

## 本包的反馈命令

```sh
python scripts/catalog.py
python scripts/catalog.py --check
python -m unittest discover -s tests -v
```

运行这些命令是维护路由程序的反馈，不是要求所有用户项目采用同样流程。程序测试通过不等于已改善模型编码能力；效果声明需要真实任务对照。没有远端读取或写入结果，不能声称已同步 GitHub。

做完授权范围就交付。用户硬约束与必要安全不因“构建优先”而失效；也不以它们为借口回避可逆的工程决定。