# 维护 invariant-first

本文件只约束技能包维护。使用者的共同工程判断在 [RULES.md](RULES.md)。本包要提炼的是优秀工程师识别条件、作出取舍、用反馈校正的“神经反射”，不是错误集合、名人口吻或必须逐项执行的流程。

## 从真实痛点提炼判断

开放式改进必须先研究，再改指令；不要先写一套规范，再搜索支持它的引用。已定位的局部缺陷直接修复，不为明确修复补做无关研究。

1. 从不同论坛、社区和作者博客读取原始经验，保留任务、约束、实际结果及相反意见。搜索摘要只作线索；未读正文、未保存日志或未独立复现时明确限制。自报告不是发生率，模型对自身行为的解释不是成因证据。
2. 带着问题对照优秀工程师的作者说明和成熟仓库的版本化源码。读到承担该责任的表示、接口、执行路径与验证方法；比较它们为什么作出不同取舍，而不是按名气选答案或照搬目录、工具和代码规模。项目是协作成果，局部样本不代表完整审计。
3. 将表面不同的痛点归并为条件、责任和因果关系：什么线索值得注意，何种动作能保持目标语义，什么反馈会改变决定，何时已经足够。写成能直接指导行动的短判断，不要求每轮输出这些分析。去掉案例名称仍能迁移；改变承诺、风险或事实时应能换一种做法。
4. 用未参与设计的任务及条件对照检查收益与误用，方法见 [迁移评估](references/evaluation.md)。区分已执行的程序检查、待执行的模型实验和设计推断；不把测试数量、规则数量、生成字数或来源热度当效果。

优先替换重复、过窄或失去条件的表述。共同判断只在 RULES 维护，子技能只保留独有行动与完成条件；案例留在参考材料，不一事一禁令、一帖一专项。新增技能须同时有独立触发和独立行动。已有方法能解释新案例，就补依据或不改；不能改变行动的材料不进入运行时。

## 修改落点

- 目标、事实、阶段、路径与优先级：`config/routing.json`；根方法地图、MASTER-ROUTING 与 INDEX 由 `scripts/catalog.py` 生成，不登记每种抱怨。
- 行动方法：`RULES.md` 与 `skills/if-*/SKILL.md`；按实际难点加载，保留跨 harness 的语义能力，不引入强制运行时。
- 依据：更新已有参考文件中支持当前取舍的最短解释、原始来源与适用边界，不滚动建立问题编号或每轮材料档案。研究不是使用本包的前置任务。
- 评估：程序回归保护接口，行为对照判断迁移；参与设计的例子只能作回归，不能再冒充保留任务。

## 完成修改

本仓库后续修改按用户授权直接提交 `main`，除非用户另行指定；不默认另开分支或 PR。提交前核对最新 `main` 并保留并发修改，不强推覆盖历史。只清理已确认合并且头提交未变化的分支，未合并工作保留。此约定不授予使用者其他项目的提交、删除或发布权限。

用实际最终差异更新说明，单入口版同源导出。源码行为、检查结果与模型效果分别说明。

```sh
python scripts/catalog.py --check
python -m unittest discover -s tests -v
```

修改路由登记后先运行 `python scripts/catalog.py`。以上是维护本包的反馈命令，不注入使用本包的项目。生成物不带版本库历史或缓存，不另建手工维护副本。
