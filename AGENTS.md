# 维护 invariant-first

本文件只约束技能包维护。使用者的共同工程判断在 [RULES.md](RULES.md)：自主完成授权目标，架构清楚，文档可用，下一会话接得住。

## 从材料提炼方法

开放式改进先读不同社区的原始反馈，再对照成熟仓库的版本化源码与作者说明。帖子用于发现疑问，源码用于理解机制；自报告不是发生率，名人项目也不是通用正确性的证明。已定位的局部缺陷直接修复。

将材料归并为已有职责中的因果关系：什么条件成立，谁负责，为什么这个动作能推进目标。去掉模型名、框架名和故事主角后，判断仍应能用于别的任务；改变真实承诺或风险时，动作也应随之改变。只能复述原帖子或记住一个禁令的内容，留作参考，不进入执行规则。

优先替换重复或过窄的表述。共同判断只在 RULES 维护，子技能只保留独有的行动与完成条件。新增技能须同时有独立触发和独立行动；新增来源须能解释一个当前设计决定。已有原则已覆盖的新案例无需新增条目、正则或文件，规则失去收益时收缩或删除。

## 修改落点

- 路由的目标、事实、阶段、路径与优先级：`config/routing.json`；两份视图由 `scripts/catalog.py` 生成。路由依据交付目标和当前事实，不登记每种抱怨。
- 行动方法：`skills/if-*/SKILL.md`；保留直接读取入口与跨 harness 的语义能力，不引入强制运行时。
- 依据：更新已有参考文件中支持该判断的最短解释与来源，不滚动建立问题编号或每轮材料档案。既有研究记录作为来源保留，不成为执行前置。
- 评估：保留确定性回归，行为评估用未参与规则设计的任务与条件对照，检查迁移和误用，不用更多熟悉题目代替泛化。

## 完成修改

用实际最终差异更新说明，单入口版同源导出。运行受影响检查；按当前用户授权提交指定分支，不重设默认发布权限。源码行为、检查结果与模型效果分别说明。

```sh
python scripts/catalog.py --check
python -m unittest discover -s tests -v
```

修改路由登记后先运行 `python scripts/catalog.py`。以上是维护本包的反馈命令，不注入使用本包的项目。生成物不带版本库历史或缓存，不另建手工维护副本。
