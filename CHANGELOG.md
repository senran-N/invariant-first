# 变更记录

## Unreleased

- 分层配置与环境按消费端实际解析到的值、版本和胜出来源判断是否生效；文件存在、变量已设置、参数被接受或进程正常启动都不再单独作为配置成功证据。
- 依赖/API 专项在 PATH、虚拟环境或配置覆盖会改变行为时追到实际进程；用 Claude/Cursor 原始反馈及反例、Git v2.46.0 与 pip 24.2 的配置来源/覆盖实现补充依据和条件对照，不新增路线、脚本或技能，模型效果尚未实测。
- 审查按成立条件、行为证据与实际后果组织发现；复核原问题而不是依赖新一轮是否重复报告，区分问题成立与修法成立。
- 合并审查入口的重复行动说明；修复方法先核实报告的症状、解释与建议，保留只读/修复授权边界。
- 用原始社区讨论、curl 的有效发现与注释修正、Go 控制流检查对照补充依据及评估条件；不新增路线或规则关键词，程序路由不变。
- 恢复与接续按原操作的状态和身份推进，区分等待、取结果与重放；正常长任务不因统一静默阈值被当作失败。
- 状态方法明确同一意图的重传与新意图、接收方去重范围/有效期及累计重试预算；保留无法判定的外部效果，不用重新发起掩盖未知。
- 从论坛原始反馈、作者博客与 Go/Stripe 版本化实现交叉归纳，补充条件对照并同步 recovery 行动摘要；不新增路线、专项或路由关键词，模型效果尚未实测。
- 修正导出测试的跨平台夹具：按解析后的临时路径比较身份，按明确字节写入已有文件；保留发布时机、失败恢复与内容保存的原有断言。
- 维护方法明确为原始社区/博客反馈、版本化源码对照、条件与责任归纳、迁移评估；不按案例堆禁令或模仿名人口吻。
- 共同判断以理解成本衡量简单性，以能否暴露目标错误衡量验证；架构与修复方法给出对应的具体动作，允许按真实契约调整测试。
- 行为评估从案例枚举收敛为迁移、条件变化与验证有效性，区分设计样本和保留任务；模型效果仍待独立对照。
- 本仓库后续直接提交 `main`，仅清理确认合并且未变化的分支；不改变使用者项目的授权边界。
- 在 GitHub Actions 上用 Linux/Windows 与 Python 3.10/3.13 同时验证源码包和 single-entry 运行时导出。
- single-entry 导出排除源码仓库的 `.github` 自动化元数据，避免把维护基础设施带入运行时技能包。
- 增加回归检查，确保运行时产物不包含仓库自动化目录；路由语义与方法数量保持不变。

## 2.1.8 · 2026-09-19

- 单入口导出在临时目录转换并验证，完成后再发布到目标目录；普通失败清理临时内容，支持修正后重试。
- 转换前检查资源目标冲突，保留已有说明文件，避免多个方法合并到同一个输出路径。
- 发布方法围绕使用者取得的实际产物判断完成；说明导出的串行使用与失败边界。
- 增加导出保存、发布时机和失败恢复的程序回归；共同判断、方法数量与路由选择保持不变。

## 2.1.7 · 2026-09-19

- 根入口直接展示从清单生成的方法链接，区分目标判断与资源寻址。
- 目录生成与单入口导出同步更新根方法地图，保留其他手工正文。
- 资源位置以当前加载的入口与安装清单为准，安装说明区分未登记路径、缺失文件和读取基准问题。
- 补充地图一致性、重定位、重命名与缺失资源的程序检查。既有路由选择与 17 个子方法保持不变。

## 2.1.6 · 2026-09-19

- 将 single-entry 明确为默认运行时分发：只暴露根 `invariant-first`，子方法改为按需读取的 `GUIDE.md`。
- 修正 Pi 安装说明：Pi 会递归发现嵌套 `SKILL.md`，因此源码/多入口包不应直接装入其技能目录；替换目录后用 `/reload` 或新会话重新扫描。
- 升级说明改为先替换整个 Skill 目录，避免复制覆盖留下已经删除或改名的旧子入口。
- 路由目标、判断、17 个子方法和程序选择行为不变。

## 2.1.5 · 2026-09-19

- 安全判断改按真实资源与副作用，而不是按“测试、预览、脚本”等标签；具有破坏能力的测试先隔离可丢弃资源。
- 并发与后台工作增加结果新鲜性判断：互斥只解决同时写入，晚到结果发布前还要确认目标版本或前提未变。
- 研究材料将测试误指真实数据和后台任务晚到覆盖归并为资源身份/发布前提两个机制，并用 Git 测试隔离、Git lockfile 与 Kubernetes `resourceVersion` 对照。
- 未新增路线、专项或案例关键词；程序路由行为保持不变。

## 2.1.4 · 2026-09-19

- 区分理解范围与写入范围：检索到或依赖于目标的代码不自动成为改动对象，写入由因果必要性和责任决定。
- 将可观察中间态纳入状态判断：只有 watcher、并发读者或外部系统能看见过程时，才要求原子/分阶段发布。
- 恢复与跨会话接续按修改来源保护已有工作，不用旧摘要或本次 Agent 的基线覆盖人工、远端或其他执行者变化。
- 研究材料继续按机制归并，以 Git 的本地修改保护和 SQLite 的原子提交为对照，不新增案例专用技能或路由词。

路由接口与程序行为保持不变。

## 2.1.3 · 2026-09-19

- 共同判断区分目标、约束与根因假设，按改变的前提调整决定。
- 修复与恢复先判断反馈的作用对象，用有区分力的观察推进实现。
- 跨会话接续按关键前提复用认识，局部更新受影响部分。
- 在现有依据中补充 Git 与 SQLite 的判别和失效机制，更新行为对照维度。

路由接口与程序行为保持不变。

## 2.1.2 · 2026-09-19

- 共同规则收敛为六项工程判断，开发、修复、演进与架构模块集中保留独有行动。
- 维护方法按条件、责任与因果关系提炼规则；研究依据和经验回流围绕当前判断组织。
- 行为评估采用跨场景迁移与条件对照，分别检查复用和区别处理。
- 保持全部主路线、专项、请求字段和分派行为；安装与单入口导出方式不变。

## 2.1.1 · 2026-09-19

- 结构编辑区分移动、重命名与语义重写，新增中英文候选及对应路由用例。
- 开发沿实际同类入口复用适用的权限、审计与业务责任；重构保留单位、精度、顺序和时间语义。
- 修复反馈经过被改逻辑的真实边界，预期来自契约或独立参考。
- 注释面向当前语义，提交与发布说明面向最终差异；保留有用的迁移与历史说明。
- 增加社区原始反馈与版本化源码的对应材料，并将研究方法写入维护入口。
- 修正生成路由表的末尾换行，保持目录视图与生成器输出一致。

## 2.1.0 · 2026-09-19

### 路由与上下文

- 将 handoff / recovery 标记为 prelude：先恢复事实和进展，但不消耗领域专项预算，避免真正的状态、契约等责任被挤到延后列表。
- 领域专项继续受 `max_support_now` 控制；CAPABILITIES 只把本轮实际加载的方法当当前需求。

### Agent 可读性与仓库卫生

- 入口文档保持为地图，稳定事实留在最接近责任的位置；重复 Agent 失败优先修表示、边界、工具或仓库说明，而不是无限追加全局提示。
- 新增“无残影”规则：被否决且无长期约束意义的方案，从实现、命名、注释、文档、测试措辞和 PR 表述中一起移除。负面约束只在协议、安全、兼容或已确认事故等长期依据存在时保留。
- 明确 harness 规则也要可删除；模型或工具能力提升后，收缩不再产生净收益的历史 workaround。
- review 对主观质量先使用可观察标准；已有独立 reviewer/evaluator 时可以利用，但不把多 Agent 变成普通任务的默认流程。

本次仍不增加子技能数量。改变的是路由预算语义和已有技能的判断规则，不据此宣称所有模型或 harness 的实际质量已提升。

## 2.0.1 · 2026-09-19

### 行动与路由

- 普通任务按目标直接读取方法；路由脚本用于批量、集成或诊断，不再默认要求每次运行。
- 接续专项排在即时专项之前，避免恢复上下文被领域工作挤到延后列表。失速恢复之后继续原 PRIMARY，不创建新任务。
- `CAPABILITIES.preferred` / `missing` 只描述 PRIMARY 与 LOAD_NOW；新增 `CAPABILITIES.deferred` 列出后续专项独有的能力。DEFERRED 仍须在处理对应边界前加载。
- 明确事实只覆盖本次改动、临时阻塞会过期，以及 SUPPORT 不扩大 PRIMARY 的写入权限或交付目标。

### 分发与维护

- 单入口导出排除 `.git`、`.hg`、`.svn` 与 Python 缓存，保留 `.gitignore` 等项目文件，不改动源目录。
- 新增仓库级 `AGENTS.md`，保存初衷、真实修改落点与反馈命令；不把维护规则注入所有用户任务。
- 增加针对性回归情形；单入口对照检查全部路由输出语义，不再只对照主路线名称。

本次不增加子技能数量，不改变公开意图和事实名。SUPPORT 顺序与 CAPABILITIES 的即时范围有上述调整。改变的是程序与指令，不据此宣称真实模型效果已提升。